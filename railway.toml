[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "python -m src.mcp_local_rag.simple_http_server"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"

[env]
PORT = "8001"
HOST = "0.0.0.0"
PYTHONPATH = "/app/src"
